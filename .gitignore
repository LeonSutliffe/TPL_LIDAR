# colcon build artifacts -- regenerated by `colcon build`, not source
ros2_ws/build/
ros2_ws/install/
ros2_ws/log/

# ESP-IDF build artifacts and vendored components under the abandoned
# firmware/ (kept for reference only, not built by anything in the current
# architecture -- see HANDOFF.md). micro_ros_espidf_component alone is a
# 2.1GB vendored third-party checkout, not project-authored code.
firmware/*/build/
firmware/*/managed_components/
firmware/tilt_controller/components/micro_ros_espidf_component/

# Python
__pycache__/
*.pyc
*.egg-info/

# OS cruft
Thumbs.db
.DS_Store

# Claude Code session-local runtime state, not project config
.claude/scheduled_tasks.lock
